using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PaletteSliderScript : MonoBehaviour
{
    //1 for left
    //2 for right
    public int slideType;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
